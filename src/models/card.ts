class Card {
  constructor(
    id: number,
    title: string,
    description: string,
    columnId: string,
    userId: string
  ) {
    id;
    title;
    description;
    columnId;
    userId;
  }
}

export default Card;
